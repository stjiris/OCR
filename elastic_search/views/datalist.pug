datalist(id=encodeURIComponent(id))
    for agg in aggs 
        if agg.doc_count
            option(value=`${agg.key}`) Quantidade de registos: !{agg.doc_count}
        else
            option(value=`${agg.key}`) !{agg.key}